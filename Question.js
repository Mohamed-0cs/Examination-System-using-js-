class Question {
    constructor(title, image, options, correctAnswer) {
        this.title = title;
        this.image = image;
        this.options = options;
        this.correctAnswer = correctAnswer;
    }
}
